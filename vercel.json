{
  "version": 2,
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["src/**/*.ts"],
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/main.js",
      "headers": {
        "Access-Control-Allow-Origin": "*"
      },
      "methods": ["GET", "POST", "DELETE", "PUT", "PATCH", "OPTIONS"]
    }
  ]
}
